# Repo Policy

Before editing code:
- search the repository for related files
- understand existing structure
- prefer modifying existing code rather than rewriting

Avoid:
- large refactors unless requested
- creating duplicate utilities